nginx:
  image: nginx
  volumes:
   - .:/code
   - docker/nginx/conf.d:/etc/nginx/conf.d:ro
   - /etc/localtime:/etc/localtime:ro
   - docker/nginx/var/log/nginx:/var/log/nginx:rw
  ports:
   - "8096:80"
  links:
   - fpm
fpm:
  build: docker/fpm/build
  volumes:
   - .:/code
   - /etc/localtime:/etc/localtime:ro
   - docker/fpm/var/log/nginx:/var/log/fpm:rw
  links:
   - mysql
mysql:
  image: mysql:5.7
  volumes:
   - docker/mysql/db:/var/lib/mysql
   - docker/mysql/etc/mysql:/etc/mysql:ro
   - docker/mysql/db_backup:/var/backups/mysql:rw
   - /etc/localtime:/etc/localtime:ro
  environment:
   MYSQL_ROOT_PASSWORD: toor
   MYSQL_USER: humhub
   MYSQL_PASSWORD: humhub
   MYSQL_DATABASE: humhub


